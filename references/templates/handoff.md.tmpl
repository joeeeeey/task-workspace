# Handoff Prompt

You are continuing a repository task managed by Agent Harness.

Repository root:

```text
{{ repo_root }}
```

Task memory:

```text
{{ task_dir_rel }}
```

Task:

{{ title }}

Instructions:

1. Work from the repository root, not from the task memory directory.
2. Read `.agent-harness/repo-profile.md`.
3. Read this task's `goal.md`, `status.md`, `decisions.md`, `open-questions.md`, and `repo-context.md`.
4. Continue from `status.md`; do not restart from scratch.
5. Keep this session scoped to this task unless the user explicitly asks otherwise.
6. After meaningful progress, update `status.md`; update `decisions.md` and `open-questions.md` when needed.
7. Do not store secrets in `.agent-harness/`.

