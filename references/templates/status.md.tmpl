# Status

Updated: {{ created_at }}

## Current State

- Task workspace created.
- Source repo context captured.
- Task-local `AGENTS.md` and `CLAUDE.md` created.
- /tmp code workspace target planned: `{{ worktree_repo }}`

## Completed

- Initialized task workspace at `{{ task_dir }}`.
- Captured source repo branch/status/diff summary.
- Captured explicit artifacts:

{{ artifact_block }}

- Captured patch artifacts:

{{ patch_block }}

## Next Steps

1. Read `AGENTS.md`, `CLAUDE.md`, `goal.md`, `context.md`, `source-repo.md`, and `worktree.md`.
2. Prepare the /tmp code workspace:

   ```bash
   {{ prepare_command }}
   ```

3. Work from the /tmp code workspace, not from this task workspace and not from the source repo root.
4. Update this `status.md` after meaningful progress.

## Blockers

- None recorded.
