{
  "task_id": "{{ task_id }}",
  "owner": "{{ owner }}",
  "created_at": "{{ created_at }}",
  "updated_at": "{{ created_at }}",
  "status": "active",
  "source_repo_root": "{{ source_repo_root }}",
  "task_workspace": "{{ task_dir }}",
  "worktree_repo": "{{ worktree_repo }}",
  "notes": "Single active owner is recommended. If another session takes over, update this file."
}
