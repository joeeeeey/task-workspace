# Goal

Task: {{ title }}
Created: {{ created_at }}

## User Request

{{ brief }}

## Source Repo

- Source root: `{{ source_repo_root }}`
- Source branch: `{{ source_branch }}`
- Remote URL: `{{ source_remote_url }}`

## Task Workspace

This directory is the task workspace:

```text
{{ task_dir }}
```

## Code Worktree

Code edits should happen in the /tmp worktree clone:

```text
{{ worktree_repo }}
```

Prepare it with:

```bash
{{ clone_command }}
```

## Acceptance Criteria

- The requested goal is completed or a clear blocker is recorded.
- Code changes, if any, happen in the /tmp worktree clone.
- Relevant commands/tests are run or explicitly marked as not run.
- `status.md` reflects the final state.

